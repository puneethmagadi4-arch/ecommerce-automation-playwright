{
  "name": "ecommerce-framework-fresh",
  "version": "1.0.0",
  "description": "Layered BDD UI test framework for nopcommerce demo",
  "scripts": {
    "test": "cucumber-js features/*.feature --config cucumber.js",
    "test:headed": "cross-env PLAYWRIGHT_HEADLESS=false npm run test",
    "test:video": "cross-env PLAYWRIGHT_HEADLESS=false cross-env PLAYWRIGHT_VIDEO_MODE=on npm run test",
    "test:debug": "cross-env PLAYWRIGHT_HEADLESS=false cross-env DEBUG=pw:api npm run test",
    "test:fast": "cross-env PLAYWRIGHT_FAST=true npm run test:headed",
    "test:slow": "cross-env PLAYWRIGHT_FAST=false npm run test:headed",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rimraf dist reports test-results",
    "clean:all": "rimraf dist reports test-results node_modules package-lock.json",
    "verify": "tsc --noEmit",
    "install:clean": "npm run clean:all && npm install",
    "start": "npm run test:video"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^10.3.1",
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.8.10",
    "cross-env": "^7.0.3",
    "cucumber-html-reporter": "^6.0.0",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {}
}
